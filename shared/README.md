# shared/

Code shared between `app/` and `server/` (types, utils) per Nuxt 4's
default layout. Auto-imported on both sides. Use for cross-feature
contracts only — feature-local code stays under
`app/features/<feature>/`.
