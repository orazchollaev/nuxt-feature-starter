# server/

Nitro server routes/middleware. Not covered by the feature-based hooks —
place cross-feature API endpoints here as `server/api/<feature>/*.ts`,
mirroring the feature name under `app/features/`.
